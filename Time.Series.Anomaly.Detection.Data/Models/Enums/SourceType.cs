using System.ComponentModel.DataAnnotations;

namespace Time.Series.Anomaly.Detection.Data.Models.Enums
{
    public enum SourceType
    {
        [Display(Name = "Graylog")]
        Graylog

        // More to come
    }
}
